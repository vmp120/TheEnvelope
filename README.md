**How to run Model:**

1) Run data_collection.py to collect and merge data from all 3 sources
2) Run etl.py to run ETL pipeline to clean and transform data
3) Run features.py to get features that will be used in the prediction model